package lk.sliit.fitnesscenter.fitnesscenterproject.servlet;import jakarta.servlet.ServletException;import jakarta.servlet.annotation.WebServlet;import jakarta.servlet.http.HttpServlet;import jakarta.servlet.http.HttpServletRequest;import jakarta.servlet.http.HttpServletResponse;import java.io.BufferedReader;import java.io.FileReader;import java.io.IOException;import java.nio.file.Paths;import java.util.ArrayList;import java.util.List;@WebServlet("/ChatBotServlet")public class ChatBotServlet extends HttpServlet {    private static final String BASE_PATH = System.getProperty("user.home") + "/FitnessCenter/";    private static  String[] SUGGESTED_QUESTIONS = {            "What are the elements in the gym?",            "What are the branches?"    };    @Override    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        String userQuery = request.getParameter("query").trim().toLowerCase();        StringBuilder botResponse = new StringBuilder();        // Handle predefined questions        if (userQuery.contains("who are you") || userQuery.contains("about iron forge") || userQuery.contains("hi")) {            botResponse.append("We are Iron Forge Fitness Center, the best in the country, offering premium facilities and elite trainers to help you achieve your fitness goals.");            SUGGESTED_QUESTIONS = new String[]{                    "Who are trainers?"            };        }        else if (userQuery.contains("who are trainers") || userQuery.contains("trainers")) {            botResponse.append(getTrainers());            SUGGESTED_QUESTIONS = new String[]{                    "Give me class Schedules"            };        }        else if (userQuery.contains("class schedule") || userQuery.contains("classes") || userQuery.contains("class") || userQuery.contains("give me class schedules")) {            botResponse.append(getClassSchedules());            SUGGESTED_QUESTIONS = new String[]{                    "What are the membership plans?",            };        }        else if (userQuery.contains("membership plans") || userQuery.contains("plans") || userQuery.contains("what are the membership plans")) {            botResponse.append(getMembershipPlans());            SUGGESTED_QUESTIONS = new String[]{                    "What are the elements in the gym?",            };        }        else if (userQuery.contains("elements in gym") || userQuery.contains("facilities") || userQuery.contains("elements") || userQuery.contains("what are the elements in the gym")) {            botResponse.append(getGymElements());            SUGGESTED_QUESTIONS = new String[]{                    "What are the branches?",            };        }        else if (userQuery.contains("branches") || userQuery.contains("what are the branches")) {            botResponse.append(getBranches());        }        else if (userQuery.contains("how to register") || userQuery.contains("register")) {            botResponse.append("Press register button to register.");        }        else {            botResponse.append("Sorry, I didn't understand your question. Please try asking about trainers, class schedules, membership plans, gym elements, or branches.");        }        // Append suggested questions        botResponse.append("\n\nYou can also ask:\n");        for (String suggestion : SUGGESTED_QUESTIONS) {            botResponse.append("- ").append(suggestion).append("\n");        }        response.setContentType("text/plain");        response.getWriter().write(botResponse.toString());    }    private String getTrainers() {        List<String> trainers = readFile("trainer.txt");        if (trainers.isEmpty()) return "No trainers available.";        StringBuilder response = new StringBuilder("Our elite trainers:\n");        for (String trainer : trainers) {            String[] parts = trainer.split(",");            if (parts.length >= 5) {                response.append("- ").append(parts[1]).append(" (Specialty: ").append(parts[4]).append(")\n");            }        }        return response.toString();    }    private String getClassSchedules() {        List<String> classes = readFile("classes.txt");        if (classes.isEmpty()) return "No classes scheduled.";        StringBuilder response = new StringBuilder("Class schedules:\n");        for (String classInfo : classes) {            String[] parts = classInfo.split(",");            if (parts.length >= 3) {                response.append("- ").append(parts[1]).append(" at ").append(parts[2]).append("\n");            }        }        return response.toString();    }    private String getMembershipPlans() {        List<String> plans = readFile("plans.txt");        if (plans.isEmpty()) return "No membership plans available.";        StringBuilder response = new StringBuilder("Membership plans:\n");        for (String plan : plans) {            String[] parts = plan.split(",");            if (parts.length >= 3) {                response.append("- ").append(parts[1]).append(": $").append(parts[3]).append("\n");            }        }        return response.toString();    }    private String getGymElements() {        List<String> elements = readFile("elements.txt");        if (elements.isEmpty()) return "No gym elements listed.";        StringBuilder response = new StringBuilder("Gym elements:\n");        for (String element : elements) {            response.append("- ").append(element).append("\n");        }        return response.toString();    }    private String getBranches() {        List<String> branches = readFile("branches.txt");        if (branches.isEmpty()) return "No branches listed.";        StringBuilder response = new StringBuilder("Our branches:\n");        for (String branch : branches) {            response.append("- ").append(branch).append("\n");        }        return response.toString();    }    private List<String> readFile(String fileName) {        List<String> lines = new ArrayList<>();        try (BufferedReader reader = new BufferedReader(new FileReader(Paths.get(BASE_PATH, fileName).toString()))) {            String line;            while ((line = reader.readLine()) != null) {                if (!line.trim().isEmpty()) {                    lines.add(line);                }            }        } catch (IOException e) {            e.printStackTrace();        }        return lines;    }}